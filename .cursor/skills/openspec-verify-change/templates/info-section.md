### К сведению

- <Замечание в формулировке «архитектор — разработчику», 1 строка с конкретикой 1С>
- <Замечание в формулировке «архитектор — разработчику», 1 строка>
Подробности — в `reports/<file>.md` (если нужен технический контекст)

**Запреты в этом блоке** (так как он попадает и в файл, и в чат):

- Имена `Layer N`, `SUGGESTION`, `WARNING`, `INFO` как ярлыки в начале строки — только в `## Технический аудит` секции отчёта.
- Внутренний словарь движка (`snapshot`, `novelty`, `phantom-symbol`, `slice coherence`, `code-truth`, `design-challenge`, `task-readiness`, «согласованность», «реализуемость», «когерентность») — переформулировать на язык кода 1С.
- Каждая строка — **конкретная**: имя процедуры, модуля, директивы, реквизита в backticks. «Замечание по согласованности» — не строка для этого блока.

<!-- Для `/opsx:verify` финальное «Как ответить» не используется — пользователь отвечает свободным текстом. Этот файл — опциональный блок «К сведению» в составе отчёта и в чате. -->
