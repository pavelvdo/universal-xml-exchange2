---
name: /opsx:continue
id: opsx-continue
category: Workflow
description: "УСТАРЕЛО — используйте /opsx:new <name> (resume)"
---

**Команда объединена с `/opsx:new`.** Дозавершение артефактов — тот же skill:

```
/opsx:new <name>
```

**Первое действие:** прочитать `.cursor/skills/openspec-new-change/SKILL.md` (режим resume, шаг 2).
