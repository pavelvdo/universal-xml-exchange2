---
verify_mode: pre-apply
verify_depth: incremental
change: universal-xml-exchange2
date: 2026-06-25
verdict: NO-GO
scope: S1 acceptance — runtime defect РасписаниеЗаполнено
snapshot:
  trigger: user runtime — ЗначениеЗаполнено on mutable schedule at form open
  accepted_tasks: S1.1-S1.11 [x], S1.accept [ ]
---

## Резюме для разработчика

**universal-xml-exchange2 — apply по срезу S1 пока нельзя:** при открытии формы элемента `рг_НастройкиОбменаXML` падает `РасписаниеЗаполнено` — вызов `ЗначениеЗаполнено(РасписаниеЗначение)` для типа `РасписаниеРегламентногоЗадания` (мутабельное значение).

**Verified root cause** (стек пользователя + код): строка 187 `Form/Module.bsl`; вызов из `ПолучитьРасписаниеИзОбъектаИлиЗадания` → `ПриСозданииНаСервере` (строка 13). Платформа: «Проверка мутабельных значений на заполненность не поддерживается».

**Не связано с S1.11** (правила в хранилище) — pre-existing helper расписания; ранее не проявлялся, пока форма не открывалась на этой ветке.

## Блокер

| ID | Суть | Где |
|----|------|-----|
| B1 | `ЗначениеЗаполнено` на `РасписаниеРегламентногоЗадания` | `РасписаниеЗаполнено`, строка 187 |
| B2 | Цепочка при открытии формы | `ПриСозданииНаСервере` → `ПолучитьРасписаниеИзОбъектаИлиЗадания` → `РасписаниеЗаполнено` |

## Исправление (inside-slice rework)

В `РасписаниеЗаполнено`:
- убрать `ЗначениеЗаполнено(РасписаниеЗначение)`;
- для `Неопределено` — `Возврат Ложь`;
- иначе оставить сравнение `Строка(РасписаниеЗначение) <> Строка(Новый РасписаниеРегламентногоЗадания)` (как сейчас, строки 191–192).

Проверить вызовы `РасписаниеЗаполнено(Расписание)` (строка 107) — после fix должны работать без исключения.

## Следующий шаг

`/opsx:apply universal-xml-exchange2` — fix в `Form/Module.bsl` (задача S1.12), повторная приёмка S1.
