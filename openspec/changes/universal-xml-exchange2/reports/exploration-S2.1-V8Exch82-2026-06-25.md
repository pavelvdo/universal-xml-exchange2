# Code Exploration: S2.1 — Public API V8Exch82.epf

**Change:** universal-xml-exchange2  
**Date:** 2026-06-25  
**Verdict:** BLOCKER — API NOT verified

## Summary

`V8Exch82.epf` отсутствует в репозитории. Обработка `ргУниверсальныйОбменXML` и макет `ДвижокОбмена` не созданы (S2.2). Экспортные методы epf не верифицированы static-анализом.

## Findings

| Путь | Результат |
|------|-----------|
| `**/*.epf` | 0 файлов |
| `src/ЗУП/cfe/рг_УниверсальныйОбменXML/` | Нет DataProcessors, нет макета |
| cf `УниверсальныйОбменДаннымиXML` | Эталон API КД 2.1 (fallback B, не epf) |

### Эталон cf (verified, не контракт epf)

- `ЗагрузитьПравилаОбмена(Источник="", ТипИсточника="XMLФайл")`
- `УстановитьЗначениеПараметраВТаблице(Имя, Значение)`
- `ИнициализироватьПервоначальныеЗначенияПараметров()`
- `ВыполнитьВыгрузку()` / `ВыполнитьЗагрузку()`
- Реквизиты: `ИмяФайлаПравилОбмена`, `ИмяФайлаОбмена`

### Паттерн загрузки epf (из design, verified pattern)

1. `Обработки.ргУниверсальныйОбменXML.ПолучитьМакет("ДвижокОбмена")`
2. Запись во временный `.epf`
3. `ВнешниеОбработки.Создать(Путь)`

## Unblock paths

1. Положить `V8Exch82.epf` в репозиторий (например `tools/V8Exch82.epf`)
2. S2.2 в Конфигураторе + выгрузка в `src/` → повтор S2.1
3. Выгрузить модуль epf в `.bsl` для static-анализа

**Блокирует:** S2.9, S2.15, BSL выгрузки/загрузки через epf.
