# S2.1 — Engine Contract verified

**Change:** universal-xml-exchange2  
**Date:** 2026-06-25  
**Verdict:** verified (static, via cf source + artifact in repo)

## Artifact

| Путь | Размер | Примечание |
|------|--------|------------|
| `tools/УниверсальныйОбменДаннымиXML.epf` | 162 594 байт | Загружается в макет `ДвижокОбмена` при S2.2 |

Имя файла — `УниверсальныйОбменДаннымиXML.epf` (не `V8Exch82.epf`); функционально — внешняя обработка «Универсальный обмен данными XML» платформы 1С.

## Метод верификации

Бинарный `.epf` не разбирался в репозитории. API **verified** по исходникам cf `УниверсальныйОбменДаннымиXML` — тот же продукт КД 2.1 XML. Якоря: `ObjectModule.bsl` строки 12395, 1500, 6101, 13300, 13160, 11979.

## Engine Contract (кратко)

См. `design.md` § Engine Contract — полная таблица методов и последовательности выгрузки/загрузки.

## Unblocks

S2.9, S2.15 — можно реализовывать BSL вызовы движка по зафиксированным именам.

## Next

S2.2 — Конфигуратор: обработка + макет с `tools/УниверсальныйОбменДаннымиXML.epf` → выгрузка в `src/`.
