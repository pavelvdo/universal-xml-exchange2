# Профиль `explore-doc` — DEPRECATED

Профиль выведен из маршрутизации (отсутствует в routing-таблице `SKILL.md`) и опирался на legacy-артефакты (`analysis.md`, T-EXPLORE-DECISION), которые в Ultra-Lite explore не создаются.

**Вместо него:**
- Постановка доработки с исследованием → профиль `bug`/`question` (`profiles/bug.md`, `profiles/question.md`); финал — блок `## Постановка ЗНИ` по `../templates/handoff-block.md`.
- Понятная фича без исследования → сразу `/opsx:new` (бриф B1 на стороне new).

Файл сохранён как stub для старых ссылок; новых маршрутов не несёт.
